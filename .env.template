# Database Configuration
POSTGRES_DB=stalldb
POSTGRES_USER=stalluser
POSTGRES_PASSWORD=stallpass
POSTGRES_PORT=5432

# Kafka Configuration
KAFKA_PORT=9092
KAFKA_TOPIC_STALL_RESERVED=stall.reserved
KAFKA_TOPIC_STALL_RELEASED=stall.released

# Keycloak Configuration (OAuth2)
KEYCLOAK_ADMIN=admin
KEYCLOAK_ADMIN_PASSWORD=admin
KEYCLOAK_PORT=8080
JWT_ISSUER_URI=http://localhost:8080/realms/exhibitflow
JWT_JWK_SET_URI=http://localhost:8080/realms/exhibitflow/protocol/openid-connect/certs

# Application Configuration
APP_PORT=8081
SPRING_PROFILES_ACTIVE=default

# Flyway Configuration
FLYWAY_ENABLED=true
FLYWAY_BASELINE_ON_MIGRATE=true
